# Frontend
frontend/node_modules/
frontend/dist/

# Backend
/g5s
backend/g5s
*.out

# Embedded frontend build (regenerated by `npm run build` / `task build`).
# index.html stays tracked as a minimal placeholder so `go build` works without
# a frontend build (go:embed needs at least one matching file in dist/).
backend/internal/webui/dist/assets/

# Direnv
.direnv/

# Design docs (kept locally, not versioned)
docs/

# Personal cluster dumps (not synthetic fixtures)
fixtures/mon-cluster.json
fixtures/my-cluster.json
